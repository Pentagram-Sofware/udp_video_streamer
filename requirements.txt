# Raspberry Pi Camera Streaming Server Dependencies

# Camera interface for Raspberry Pi
picamera2>=0.3.12

# Computer vision and image processing
opencv-python>=4.8.0

# Core Python libraries (usually included with Python)
# Listed here for completeness and virtual environments

# Network and system libraries (built-in)
# socket - built-in
# struct - built-in
# pickle - built-in
# threading - built-in
# time - built-in
# io - built-in
# socketserver - built-in

# HTTP server (built-in)
# http.server - built-in

# Optional: For development and testing
# numpy>=1.24.0  # Usually comes with opencv-python
# pillow>=9.0.0  # For additional image format support

# System requirements (install via apt/system package manager):
# - libcamera-dev
# - python3-libcamera
# - python3-kms++
# - python3-prctl
# - python3-pidfile
